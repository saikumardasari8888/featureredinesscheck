<mvc:View controllerName="ns.html5module.controller.View1"
    xmlns:mvc="sap.ui.core.mvc"
    displayBlock="true" 
    xmlns="sap.m">
	<Shell id="shell">
		<App id="app">
			<pages>
				<Page id="page" title="{i18n>title}">
					<subHeader>
						<Toolbar id="searchToolbar">
							<SearchField id="productSearch"
								width="100%"
								placeholder="{i18n>searchPlaceholder}"
								search=".onSearchProducts"
								liveChange=".onSearchProducts" />
						</Toolbar>
					</subHeader>
					<content>
						<List id="productList"
							headerText="{i18n>productListTitle}"
							items="{/products}">
							<StandardListItem
								title="{name}"
								description="{category}"
								info="{price}"
								type="Active"
								press=".onProductPress" />
						</List>
					</content>
				</Page>
			</pages>
		</App>
	</Shell>
</mvc:View>